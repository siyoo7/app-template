// Tailwind v4 — most config now lives as @theme tokens in src/index.css.
// This file is kept for tooling that still expects it; safe to leave minimal.
export default {
  content: ['./index.html', './src/**/*.{js,jsx,ts,tsx}'],
};
